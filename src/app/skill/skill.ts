export class Skill {
     id: number;
     name: string;
     side: string;
    constructor(id: number, name: string, side: string) {
        this.id = id;
        this.name = name;
        this.side = side;
    };
}